from globals import dirname, join

curr_dir = dirname(__file__)
assets_dir = join(curr_dir, "assets")
logos_dir = join(assets_dir, "logo")
fonts_dir = join(assets_dir, "fonts")
images_dir = join(assets_dir, "images")